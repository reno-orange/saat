/**
 * Rules Summary Report Generator
 * Generates a markdown report summarizing the WCAG rules tested
 */

export function generateRulesSummaryMarkdown(): string {
  return '# WCAG Rules Summary Report\n\nNo longer generated in SAAT.\n';
}
